MonoGame DesktopGL 
OpenGL
Target framework: .NET 9

Build and run it from VS Code from the menu `Run` then choose `Start Debugging` or simply click `F5`

Build and run it separately from the terminal, using the command `dotnet build` and `dotnet run` from the Sandbox folder
